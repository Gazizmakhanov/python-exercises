from operator import truediv
from pickle import TRUE


little_mermaid=3
brother_bear=5
hercules=1

payment=little_mermaid*3+brother_bear*3+hercules*3

print(payment);

google=400
amazon=380
facebook=350

paycheck=facebook*10+google*6+amazon*4

print(paycheck);

the_class='not full'
class_schedule='current_schedule'
if the_class == 'not full' and class_schedule!='current_schedule':
    enroll==true   


the_buy=15
expiration='not expired'
premium_buyer=TRUE

if the_buy>=2 and expiration=='not expired'and premium_buyer==TRUE:
    product_offer=TRUE


# Create a variable that holds a boolean value for each of the following conditions:

# the password must be at least 5 characters
# the username must be no more than 20 characters
# the password must not be the same as the username
# bonus neither the username or password can start or end with whitespace
 
username = 'codeup'
password = 'notastrongpassword'

if (len(password)>=5) & (len(username)<=20) & (password!=username):
    valid_pasword=TRUE



